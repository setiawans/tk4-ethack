<!DOCTYPE html>
<html lang="en">
<head>
    <title>Register</title>
    <link rel="stylesheet" href="/css/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex flex-col items-center justify-center h-screen bg-gray-100">
    <div class="w-96 p-6 bg-white shadow-md rounded-md">
        <h2 class="text-2xl font-bold text-center text-gray-800">Register</h2>

        <% if (error) { %>
            <p class="text-red-500 font-bold text-center mt-2"><%= error %></p>
        <% } %>

        <form action="/auth/register" method="POST" class="mt-4 space-y-4">
            <div>
                <label for="email" class="font-bold text-gray-700">Email</label>
                <input type="email" name="email" id="email" placeholder="Email" required
                    class="w-full p-2 mt-1 border rounded-md focus:ring-2 focus:ring-blue-500 font-bold">
            </div>

            <div>
                <label for="password" class="font-bold text-gray-700">Password</label>
                <input type="password" name="password" id="password" placeholder="Password" required
                    class="w-full p-2 mt-1 border rounded-md focus:ring-2 focus:ring-blue-500 font-bold">
            </div>

            <button type="submit" class="w-full bg-blue-500 text-white font-bold py-2 rounded-md hover:bg-blue-600">
                Register
            </button>
        </form>

        <p class="mt-4 text-center text-gray-700">
            Already have an account? <a href="/auth/login" class="text-blue-500 font-bold">Login</a>
        </p>
    </div>
</body>
</html>